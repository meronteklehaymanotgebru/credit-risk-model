version: "3.8"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./mlruns:/app/mlruns   # so the container sees the MLflow tracking data
    environment:
      - MLFLOW_TRACKING_URI=file:///app/mlruns